import { View, Text } from 'react-native'
import { GroupsContainer, GroupsTitle } from './styles';

export const Groups = () => {
  return (
    <GroupsContainer>
      <GroupsTitle>Open up App.tsx to start working on your app!</GroupsTitle>
    </GroupsContainer>
  );
}