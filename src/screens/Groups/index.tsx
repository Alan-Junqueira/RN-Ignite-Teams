import { Header } from '@components/partials/Header';
import { GroupsContainer } from './styles';

export const Groups = () => {
  return (
    <GroupsContainer>
      <Header />
    </GroupsContainer>
  );
}